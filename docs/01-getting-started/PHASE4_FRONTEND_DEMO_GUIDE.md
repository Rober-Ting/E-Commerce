# Phase 4 订单管理前端 Demo 使用指南

## 📖 概述

这是一个完整的订单管理系统前端 Demo，用于测试 Phase 4 的所有订单管理功能。

## 🚀 快速开始

### 1. 启动后端服务器

```powershell
.\start_backend.ps1
```

或手动启动：

```powershell
.\venv\Scripts\activate
uvicorn app.main:app --reload
```

### 2. 启动前端 Demo

```powershell
.\start_orders_demo.ps1
```

### 3. 访问 Demo

打开浏览器访问：
```
http://localhost:8080/frontend_orders_demo.html
```

## 👤 测试账户

| 角色 | 邮箱 | 密码 | 权限 |
|------|------|------|------|
| **Customer** | customer@test.com | Customer123! | 购物、下单、查看自己的订单 |
| **Vendor** | vendor@test.com | Vendor123! | 同 Customer |
| **Admin** | admin@ecommerce.com | Admin123! | 所有权限 + 查看所有订单 + 订单统计 |

## ✨ 功能特性

### 📦 商品浏览
- ✅ 浏览所有在售商品
- ✅ 查看商品详情（名称、价格、库存、描述）
- ✅ 将商品添加到购物车

### 🛒 购物车管理
- ✅ 查看购物车中的商品
- ✅ 增加/减少商品数量
- ✅ 删除购物车中的商品
- ✅ 查看购物车总金额
- ✅ 实时库存检查

### 💳 订单创建
- ✅ 填写收货地址信息
  - 收件人姓名
  - 联系电话
  - 详细地址
  - 城市、省份、邮编、国家
- ✅ 选择支付方式
  - 信用卡
  - 借记卡
  - PayPal
  - 支付宝
  - 微信支付
  - 银行转账
  - 货到付款
- ✅ 添加订单备注
- ✅ 查看订单总额
- ✅ 确认下单

### 📋 我的订单
- ✅ 查看订单列表
- ✅ 按状态筛选订单
  - 待支付
  - 已支付
  - 处理中
  - 已发货
  - 已送达
  - 已取消
- ✅ 按日期范围筛选
- ✅ 查看订单详情
  - 订单号
  - 订单状态
  - 商品清单
  - 收货地址
  - 支付方式
  - 订单金额
- ✅ 取消订单（仅待支付/已支付状态）

### 👥 所有订单（管理员专属）
- ✅ 查看所有用户的订单
- ✅ 按状态筛选
- ✅ 按用户邮箱筛选
- ✅ 更新订单状态
- ✅ 查看订单详情

### 📊 订单统计（管理员专属）
- ✅ 总订单数
- ✅ 总订单金额
- ✅ 待支付订单数
- ✅ 已完成订单数
- ✅ 已取消订单数
- ✅ 平均订单金额

## 🧪 测试场景

### 场景 1：完整购物流程（Customer）

1. **登录**
   - 使用 `customer@test.com` / `Customer123!` 登录
   
2. **浏览商品**
   - 在"商品列表"标签页浏览商品
   - 点击"加入购物车"添加喜欢的商品
   
3. **管理购物车**
   - 切换到"购物车"标签页
   - 调整商品数量或删除不需要的商品
   - 查看总金额
   
4. **创建订单**
   - 点击"结算订单"按钮
   - 填写收货地址信息
   - 选择支付方式
   - 添加订单备注（可选）
   - 点击"确认下单"
   
5. **查看订单**
   - 切换到"我的订单"标签页
   - 查看刚创建的订单
   - 点击"查看详情"查看完整订单信息

**预期结果**：
- ✅ 订单创建成功
- ✅ 购物车清空
- ✅ 商品库存自动扣减
- ✅ 订单状态为"待支付"

---

### 场景 2：订单筛选（Customer）

1. **创建多个订单**
   - 创建 3-5 个订单
   
2. **按状态筛选**
   - 在"我的订单"页面
   - 选择不同的订单状态
   - 点击"应用筛选"
   
3. **按日期筛选**
   - 选择开始日期和结束日期
   - 点击"应用筛选"

**预期结果**：
- ✅ 只显示符合筛选条件的订单
- ✅ 筛选结果准确

---

### 场景 3：取消订单（Customer）

1. **创建订单**
   - 创建一个新订单（状态为"待支付"）
   
2. **取消订单**
   - 在"我的订单"页面找到该订单
   - 点击"取消订单"
   - 输入取消原因
   - 确认取消
   
3. **验证库存**
   - 返回"商品列表"
   - 检查相关商品库存是否恢复

**预期结果**：
- ✅ 订单状态变为"已取消"
- ✅ 商品库存恢复
- ✅ 显示取消成功提示

---

### 场景 4：库存不足处理

1. **添加商品到购物车**
   - 选择一个库存较少的商品
   - 尝试添加超过库存数量的商品
   
2. **创建订单**
   - 尝试购买超过库存数量的商品

**预期结果**：
- ✅ 购物车中无法增加超过库存的数量
- ✅ 显示"库存不足"提示
- ✅ 如果库存不足，订单创建失败

---

### 场景 5：管理所有订单（Admin）

1. **登录为管理员**
   - 使用 `admin@ecommerce.com` / `Admin123!` 登录
   
2. **查看所有订单**
   - 切换到"所有订单"标签页
   - 查看所有用户的订单
   
3. **按用户筛选**
   - 输入用户邮箱（如 `customer@test.com`）
   - 点击"应用筛选"
   
4. **更新订单状态**
   - 选择一个订单
   - 点击"更新状态"
   - 输入新状态（如 `paid`, `processing`, `shipped`, `delivered`）
   - 确认更新
   
5. **查看订单详情**
   - 点击"查看详情"查看完整订单信息

**预期结果**：
- ✅ 可以看到所有用户的订单
- ✅ 筛选功能正常
- ✅ 订单状态更新成功
- ✅ 订单详情完整显示

---

### 场景 6：订单统计（Admin）

1. **创建多个订单**
   - 使用不同账户创建多个订单
   - 包括不同状态的订单（待支付、已完成、已取消）
   
2. **查看统计**
   - 以管理员身份登录
   - 切换到"订单统计"标签页
   
3. **验证数据**
   - 检查总订单数是否正确
   - 检查总金额是否正确
   - 检查各状态订单数是否正确
   - 检查平均订单金额是否正确

**预期结果**：
- ✅ 所有统计数据准确
- ✅ 实时更新

---

### 场景 7：订单状态转换（Admin）

测试订单状态的合理转换：

1. **创建新订单** → 状态：`pending`（待支付）
2. **更新为已支付** → 状态：`paid`
3. **更新为处理中** → 状态：`processing`
4. **更新为已发货** → 状态：`shipped`
5. **更新为已送达** → 状态：`delivered`

**预期结果**：
- ✅ 状态转换成功
- ✅ 订单历史记录正确

---

### 场景 8：多商品订单

1. **添加多个商品**
   - 向购物车添加 5 个不同的商品
   - 每个商品数量不同
   
2. **创建订单**
   - 填写收货信息
   - 确认下单
   
3. **查看订单详情**
   - 验证所有商品都在订单中
   - 验证数量和价格正确
   - 验证总金额正确

**预期结果**：
- ✅ 所有商品都包含在订单中
- ✅ 每个商品的库存正确扣减
- ✅ 总金额计算正确

---

## 💡 学习要点

### 1. 购物车状态管理
- 前端使用数组管理购物车状态
- 实时计算总金额
- 库存检查

### 2. 订单创建流程
- 收集用户输入（地址、支付方式）
- 构建订单数据结构
- 发送 POST 请求到后端
- 处理响应（成功/失败）

### 3. 订单列表筛选
- 使用 URLSearchParams 构建查询参数
- 支持多条件筛选
- 实时更新列表

### 4. 订单状态管理
- 不同状态的订单显示不同的操作按钮
- 状态转换的权限控制
- 订单取消的库存恢复逻辑

### 5. 权限控制
- 基于用户角色显示/隐藏功能
- Customer：只能看自己的订单
- Admin：可以看所有订单和统计数据

### 6. API 调用
- 使用 `fetch` 发送 HTTP 请求
- 设置 `Authorization` 头部传递 Token
- 处理错误响应
- 显示友好的错误提示

## 📊 API 端点对应

| 功能 | API 端点 | 方法 |
|------|----------|------|
| 获取商品列表 | `/api/v1/products` | GET |
| 创建订单 | `/api/v1/orders` | POST |
| 获取我的订单 | `/api/v1/orders/my` | GET |
| 获取所有订单 | `/api/v1/orders/all` | GET |
| 获取订单详情 | `/api/v1/orders/{id}` | GET |
| 更新订单状态 | `/api/v1/orders/{id}/status` | PUT |
| 取消订单 | `/api/v1/orders/{id}/cancel` | POST |
| 获取订单统计 | `/api/v1/orders/statistics` | GET |

## 🐛 常见问题

### Q1: 后端服务器未运行
**错误**：连接服务器失败

**解决**：
```powershell
.\start_backend.ps1
```

### Q2: 购物车数据丢失
**原因**：购物车数据存储在内存中，刷新页面会丢失

**解决**：这是正常行为，实际项目中应使用 localStorage 或后端存储

### Q3: 创建订单时库存不足
**错误**：创建订单失败

**解决**：
1. 返回商品列表查看库存
2. 减少购买数量
3. 或先取消其他待支付订单恢复库存

### Q4: 无法查看所有订单
**原因**：只有 Admin 角色才能查看所有订单

**解决**：使用 `admin@ecommerce.com` 登录

### Q5: 订单状态更新失败
**原因**：状态转换不符合业务规则

**解决**：参考订单状态机，确保状态转换合理

## 🎯 测试建议

### 基础测试
1. ✅ 创建订单成功
2. ✅ 查看订单列表
3. ✅ 查看订单详情
4. ✅ 取消订单

### 进阶测试
1. ✅ 库存不足处理
2. ✅ 订单筛选功能
3. ✅ 管理员更新状态
4. ✅ 订单统计数据

### 压力测试
1. ✅ 创建大量订单
2. ✅ 多商品订单
3. ✅ 并发库存扣减

## 📝 代码学习

### 关键代码片段

#### 1. 添加到购物车
```javascript
function addToCart(product) {
    const existingItem = cart.find(item => item.product_id === product.id);
    
    if (existingItem) {
        if (existingItem.quantity < product.stock) {
            existingItem.quantity++;
        } else {
            showMessage('❌ 库存不足', 'error');
            return;
        }
    } else {
        cart.push({
            product_id: product.id,
            product_name: product.name,
            price: product.price,
            quantity: 1,
            stock: product.stock
        });
    }
    
    updateCartBadge();
}
```

#### 2. 创建订单
```javascript
const orderData = {
    items: cart.map(item => ({
        product_id: item.product_id,
        product_name: item.product_name,
        price: item.price,
        quantity: item.quantity
    })),
    shipping_address: { /* ... */ },
    payment_method: document.getElementById('paymentMethod').value,
    note: document.getElementById('orderNote').value
};

const response = await fetch(`${API_BASE_URL}/api/v1/orders`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${currentToken}`
    },
    body: JSON.stringify(orderData)
});
```

#### 3. 订单筛选
```javascript
const params = new URLSearchParams();

const status = document.getElementById('filterOrderStatus').value;
if (status) params.append('status', status);

const startDate = document.getElementById('filterStartDate').value;
if (startDate) params.append('start_date', startDate);

const response = await fetch(`${API_BASE_URL}/api/v1/orders/my?${params}`, {
    headers: { 'Authorization': `Bearer ${currentToken}` }
});
```

## 🎉 总结

这个 Demo 展示了完整的电商订单管理流程，包括：
- ✅ 商品浏览和购物车管理
- ✅ 订单创建和地址填写
- ✅ 订单查询和筛选
- ✅ 订单状态管理
- ✅ 订单取消和库存恢复
- ✅ 订单统计（管理员）

通过这个 Demo，你可以：
1. 📖 学习前后端交互流程
2. 🧪 测试所有订单管理功能
3. 💻 研究实际代码实现
4. 🐛 发现并修复潜在问题

**开始探索吧！** 🚀

---

有任何问题，请参考：
- 📖 `docs/02-development/PHASE4_PROGRESS.md` - 开发进度
- 📖 `docs/01-getting-started/ORDER_MANAGEMENT_GUIDE.md` - API 文档
- 📖 `tests/test_phase4_orders.py` - 测试用例

