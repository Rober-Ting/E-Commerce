# 🌐 前端 JavaScript 调试指南

## 📌 重要概念

### 前端 vs 后端调试

| 特性 | 前端 JavaScript | 后端 Python |
|------|----------------|------------|
| **运行位置** | 浏览器 | 服务器（VS Code） |
| **调试工具** | 浏览器开发者工具 | VS Code 调试器 |
| **断点设置** | 浏览器 Sources 标签 | VS Code 行号左侧 |
| **快捷键** | F12 打开工具 | F5 启动调试 |
| **变量查看** | Scope 面板 / Console | Variables 面板 |

---

## 🚀 方法一：浏览器开发者工具

### 步骤 1：打开开发者工具

**快捷键：**
- Chrome / Edge: `F12` 或 `Ctrl+Shift+I`
- Firefox: `F12` 或 `Ctrl+Shift+K`
- Safari: `Cmd+Option+I`（需先在偏好设置中启用）

**或右键菜单：**
```
右键页面 → 检查 / Inspect
```

---

### 步骤 2：切换到 Sources 标签

```
开发者工具 → Sources 标签
```

---

### 步骤 3：找到文件

**左侧面板：**
```
Page
  └─ localhost:8080
      └─ frontend_demo.html  ← 点击打开
```

**快捷键：**
- `Ctrl+P` (Chrome) 或 `Ctrl+O` (Firefox) → 输入文件名

---

### 步骤 4：设置断点

1. 找到要调试的行（如第 419 行）
2. 点击行号左侧
3. 出现**蓝色圆点**表示断点已设置

**示例：在 `frontend_demo.html` 设置断点**

```javascript
// 第 407 行 - 获取输入值
const email = document.getElementById('loginEmail').value;  // ← 断点 1
const password = document.getElementById('loginPassword').value;

// 第 411 行 - 发送请求前
const response = await fetch(`${API_BASE_URL}/api/v1/auth/login`, {  // ← 断点 2
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({ email, password })
});

// 第 419 行 - 接收响应
const data = await response.json();  // ← 断点 3

// 第 421 行 - 处理结果
if (response.ok) {  // ← 断点 4
    currentToken = data.data.access_token;
}
```

---

### 步骤 5：触发执行

1. 在页面输入邮箱和密码
2. 点击"登录"按钮
3. **浏览器会自动暂停在断点处**

---

### 步骤 6：查看变量

#### **方法 1：鼠标悬停**

鼠标移到变量上，会显示当前值：
```javascript
response  ← 鼠标悬停显示：Response {ok: true, status: 200, ...}
```

#### **方法 2：Scope 面板**

右侧 Scope 面板显示所有局部变量：
```
Scope
  ├─ Local
  │   ├─ email: "rob19940528@gmail.com"
  │   ├─ password: "Robert0528@"
  │   ├─ response: Response
  │   └─ data: undefined (尚未执行)
  └─ Closure
      └─ API_BASE_URL: "http://127.0.0.1:8000"
```

#### **方法 3：Console 控制台**

在暂停状态下，可以在 Console 中输入变量名查看：
```javascript
> response
← Response {type: 'cors', url: 'http://127.0.0.1:8000/api/v1/auth/login', ...}

> response.ok
← true

> response.status
← 200
```

---

### 步骤 7：控制执行

使用顶部的控制按钮：

| 按钮 | 快捷键 | 功能 | 说明 |
|------|--------|------|------|
| ▶️ Resume | `F8` | 继续执行 | 运行到下一个断点 |
| ⤵️ Step Over | `F10` | 单步执行 | 执行当前行，不进入函数 |
| ⤴️ Step Into | `F11` | 进入函数 | 进入函数内部 |
| ⤴️ Step Out | `Shift+F11` | 跳出函数 | 执行完当前函数 |
| ⏸️ Pause | `F8` | 暂停执行 | 暂停当前脚本 |

---

## 🔧 方法二：使用 `debugger` 语句

### 直接在代码中插入

在 `frontend_demo.html` 的 `<script>` 部分添加：

```javascript
// 登录表单提交
document.getElementById('loginForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    hideMessage();

    const email = document.getElementById('loginEmail').value;
    const password = document.getElementById('loginPassword').value;
    
    debugger;  // ← 浏览器会自动暂停在这里

    try {
        const response = await fetch(`${API_BASE_URL}/api/v1/auth/login`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ email, password })
        });

        debugger;  // ← 发送请求后暂停

        const data = await response.json();
        
        debugger;  // ← 解析数据后暂停

        if (response.ok) {
            debugger;  // ← 成功时暂停
            currentToken = data.data.access_token;
            showMessage('✅ 登录成功！', 'success');
        } else {
            debugger;  // ← 失败时暂停
            showMessage('❌ ' + (data.error?.message || '登录失败'), 'error');
        }
    } catch (error) {
        debugger;  // ← 捕获错误时暂停
        showMessage('❌ 连接服务器失败', 'error');
    }
});
```

**优点：**
- ✅ 不需要在浏览器中手动设置断点
- ✅ 代码中明确标记调试位置
- ✅ 在任何浏览器都能工作

**缺点：**
- ⚠️ 使用后需要删除
- ⚠️ 如果忘记删除，生产环境会暂停

---

## 📝 方法三：使用 `console.log`

### 打印关键变量

```javascript
document.getElementById('loginForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    hideMessage();

    const email = document.getElementById('loginEmail').value;
    const password = document.getElementById('loginPassword').value;

    console.log('='.repeat(50));
    console.log('🚀 开始登录请求');
    console.log('📧 Email:', email);
    console.log('🔒 Password:', password.replace(/./g, '*'));  // 隐藏密码
    console.log('='.repeat(50));

    try {
        const response = await fetch(`${API_BASE_URL}/api/v1/auth/login`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ email, password })
        });

        console.log('📡 响应接收:');
        console.log('  ✓ Status:', response.status);
        console.log('  ✓ OK:', response.ok);
        console.log('  ✓ Headers:', [...response.headers.entries()]);

        const data = await response.json();
        
        console.log('📦 响应数据:');
        console.log('  ✓ Success:', data.success);
        console.log('  ✓ Data:', data.data);
        console.log('  ✓ Error:', data.error);

        if (response.ok) {
            console.log('🎉 登录成功！');
            console.log('🔑 Token:', data.data.access_token.substring(0, 20) + '...');
            console.log('👤 用户:', data.data.user);
            currentToken = data.data.access_token;
            showMessage('✅ 登录成功！', 'success');
        } else {
            console.log('❌ 登录失败！');
            console.log('🚫 错误代码:', data.error?.code);
            console.log('🚫 错误消息:', data.error?.message);
            console.log('🚫 错误详情:', data.error?.details);
            showMessage('❌ ' + (data.error?.message || '登录失败'), 'error');
        }
    } catch (error) {
        console.error('💥 捕获异常:', error);
        console.error('💥 错误堆栈:', error.stack);
        showMessage('❌ 连接服务器失败', 'error');
    }
    
    console.log('='.repeat(50));
});
```

**Console 输出示例：**

```
==================================================
🚀 开始登录请求
📧 Email: rob19940528@gmail.com
🔒 Password: ***********
==================================================
📡 响应接收:
  ✓ Status: 200
  ✓ OK: true
  ✓ Headers: [["content-type", "application/json"], ...]
📦 响应数据:
  ✓ Success: true
  ✓ Data: {access_token: "eyJhbG...", user: {...}}
  ✓ Error: undefined
🎉 登录成功！
🔑 Token: eyJhbGciOiJIUzI1NiI...
👤 用户: {email: "rob19940528@gmail.com", role: "customer", ...}
==================================================
```

---

## 🎯 实际调试示例

### 示例 1：调试登录失败

**问题：** 登录总是失败，不知道哪里出错

**步骤：**

1. **在第 419 行设置断点**
   ```javascript
   const data = await response.json();  // ← 断点
   ```

2. **触发登录**
   - 输入邮箱和密码
   - 点击登录按钮

3. **暂停后查看 `response`**
   ```javascript
   > response.ok
   ← false  // ❌ 失败
   
   > response.status
   ← 401  // 未授权
   ```

4. **继续执行，查看 `data`**
   ```javascript
   > data
   ← {
       success: false,
       error: {
         code: "UNAUTHORIZED",
         message: "Incorrect email or password"
       }
     }
   ```

5. **结论：** 密码错误

---

### 示例 2：调试网络请求

**问题：** 不确定请求是否正确发送

**步骤：**

1. **切换到 Network 标签**
   ```
   开发者工具 → Network 标签
   ```

2. **触发登录**
   - 输入邮箱和密码
   - 点击登录按钮

3. **查看请求详情**
   ```
   Name: login
   Status: 200
   Type: fetch
   ```

4. **点击请求查看详情**
   - **Headers 标签**: 查看请求头
   - **Payload 标签**: 查看请求体
   - **Response 标签**: 查看响应内容
   - **Timing 标签**: 查看请求时间

**Request Payload:**
```json
{
  "email": "rob19940528@gmail.com",
  "password": "Robert0528@"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "access_token": "eyJhbGci...",
    "user": {...}
  }
}
```

---

## 🎓 调试技巧

### 技巧 1：条件断点

只在特定条件下暂停：

1. 右键断点 → **Edit Breakpoint**
2. 输入条件：`response.status === 401`
3. 只有当状态码为 401 时才暂停

---

### 技巧 2：日志断点

不暂停，只打印日志：

1. 右键断点 → **Edit Breakpoint**
2. 选择 **Logpoint**
3. 输入日志内容：`Response status: {response.status}`

---

### 技巧 3：监视表达式

持续监视变量值：

1. 右侧 **Watch** 面板
2. 点击 **+** 添加表达式
3. 输入：`response.ok`、`data.success`

---

### 技巧 4：黑盒脚本

忽略第三方库代码：

1. 在 Call Stack 中右键某个文件
2. 选择 **Add script to ignore list**
3. Step Into 时会跳过该文件

---

## 🔗 前后端联合调试

### 完整调试流程

```
┌─────────────────────────────────────────┐
│ 1. 前端断点（浏览器 F12）               │
├─────────────────────────────────────────┤
│ frontend_demo.html:407                  │
│ const email = ...                       │
│ ↓ 查看用户输入                          │
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 2. 前端断点（浏览器）                   │
├─────────────────────────────────────────┤
│ frontend_demo.html:411                  │
│ const response = await fetch(...)       │
│ ↓ 发送请求前                            │
└─────────────────────────────────────────┘
                ↓ HTTP POST
┌─────────────────────────────────────────┐
│ 3. 后端断点（VS Code F5）               │
├─────────────────────────────────────────┤
│ app/api/v1/auth.py:86                   │
│ logger.info(f"登入請求...")             │
│ ↓ 接收请求                              │
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 4. 后端断点（VS Code）                  │
├─────────────────────────────────────────┤
│ app/services/user_service.py:236        │
│ user = await self.get_user_by_email()   │
│ ↓ 查询用户                              │
└─────────────────────────────────────────┘
                ↓ 返回响应
┌─────────────────────────────────────────┐
│ 5. 前端断点（浏览器）                   │
├─────────────────────────────────────────┤
│ frontend_demo.html:419                  │
│ const data = await response.json()      │
│ ↓ 接收响应                              │
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 6. 前端断点（浏览器）                   │
├─────────────────────────────────────────┤
│ frontend_demo.html:421                  │
│ if (response.ok)                        │
│ ↓ 处理结果                              │
└─────────────────────────────────────────┘
```

---

## 📚 相关文档

- [后端调试指南](DEBUG_LOGIN_GUIDE.md)
- [VS Code 调试指南](VSCODE_DEBUG_GUIDE.md)
- [Chrome DevTools 文档](https://developer.chrome.com/docs/devtools/)
- [Firefox 开发者工具](https://firefox-source-docs.mozilla.org/devtools-user/)

---

## 🎉 总结

### 关键要点

1. **前端 JavaScript 在浏览器中调试**
   - 使用 F12 打开开发者工具
   - 在 Sources 标签设置断点
   - 使用 Console 查看变量

2. **后端 Python 在 VS Code 中调试**
   - 使用 F5 启动调试
   - 在代码行号左侧设置断点
   - 使用 Variables 面板查看变量

3. **`debugger` 语句**
   - 可以在代码中直接设置断点
   - 记得使用后删除

4. **`console.log`**
   - 最简单的调试方法
   - 适合快速查看变量值

---

**最后更新**: 2025-11-11  
**作者**: AI Assistant

**祝调试顺利！** 🎊



